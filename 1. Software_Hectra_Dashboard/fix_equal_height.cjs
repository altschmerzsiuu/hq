const fs = require('fs');
const path = require('path');

const targetPath = path.join(__dirname, 'src/components/shared/CowAnalyticsView.jsx');
// Wait, we are editing DetailTernak.jsx, not CowAnalyticsView.jsx!
