# Target URL for Vite Dev Server Proxy (used in local development)
VITE_API_TARGET=http://localhost:5000

# Base URL for API calls in the browser (optional, defaults to '/api' if left empty)
VITE_API_URL=
